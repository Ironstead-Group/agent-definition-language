# Minimal valid ADL document (v0.1.0-draft)
# Only required fields: adlVersion, info (title, version)

adlVersion: "0.1.0"
info:
  title: Minimal Example Agent
  description: A minimal agent definition for testing and illustration.
  version: "1.0.0"
  license:
    name: Apache-2.0
